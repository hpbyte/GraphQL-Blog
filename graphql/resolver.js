const Post = require('../models/post');
const Tag = require('../models/tag');
const User = require('../models/user');

const resovler = {
	// Queries
	// get all posts
	posts: () => {
		
	},
	// get a specific post based on id
	post: (args) => {
		
	},
	// get all tags
	tags: () => {
		
	},
	// get a specific tag based on id
	tag: (args) => {
		
	}
};

module.exports = resovler;
